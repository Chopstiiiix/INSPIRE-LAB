# =============================================================================
# INFRASTRUCTURE ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env and fill in the values before running docker-compose

# =============================================================================
# MATRIX SYNAPSE
# =============================================================================

# Server configuration
MATRIX_SERVER_NAME=matrix.yourdomain.com
MATRIX_PUBLIC_BASEURL=https://matrix.yourdomain.com

# Database
MATRIX_DB_PASSWORD=change_me_to_a_secure_password

# Secrets (generate with: openssl rand -hex 32)
MATRIX_MACAROON_SECRET_KEY=generate_with_openssl_rand_hex_32
MATRIX_FORM_SECRET=generate_with_openssl_rand_hex_32
MATRIX_REGISTRATION_SHARED_SECRET=generate_with_openssl_rand_hex_32

# =============================================================================
# LIVEKIT
# =============================================================================

# API credentials (generate key with: openssl rand -base64 12 | tr -d '=+/')
# Generate secret with: openssl rand -base64 32
LIVEKIT_API_KEY=APIxxxxxxxxxxxxxxxx
LIVEKIT_API_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Server URLs
LIVEKIT_DOMAIN=livekit.yourdomain.com
LIVEKIT_WS_URL=wss://livekit.yourdomain.com

# =============================================================================
# TURN SERVER (coturn)
# =============================================================================

# Domain and credentials
TURN_DOMAIN=turn.yourdomain.com
TURN_SECRET=generate_with_openssl_rand_hex_32

# External IP (required for NAT traversal - set to your server's public IP)
TURN_EXTERNAL_IP=YOUR_PUBLIC_IP_ADDRESS

# =============================================================================
# ELEMENT WEB (Optional - for admin debugging)
# =============================================================================

ELEMENT_WEB_ENABLED=false
