# =============================================================================
# INSPIRE-LAB / KUDO Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values
#
# For infrastructure setup, see:
#   - infra/README.md (overview)
#   - infra/matrix/README.md (Matrix Synapse)
#   - infra/livekit/README.md (LiveKit + TURN)
# =============================================================================

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
DATABASE_URL="postgresql://user:password@localhost:5432/inspire_lab"

# -----------------------------------------------------------------------------
# NextAuth
# -----------------------------------------------------------------------------
NEXTAUTH_URL="http://localhost:3000"
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=""

# -----------------------------------------------------------------------------
# Email (optional - if not set, credentials auth will be used)
# -----------------------------------------------------------------------------
RESEND_API_KEY=""
EMAIL_FROM="noreply@inspire-lab.app"

# -----------------------------------------------------------------------------
# UploadThing (File uploads)
# -----------------------------------------------------------------------------
UPLOADTHING_SECRET=""
UPLOADTHING_APP_ID=""

# =============================================================================
# MATRIX SYNAPSE (Chat)
# =============================================================================
# See infra/matrix/README.md for setup instructions

# Base URL of your Synapse server
# Development: http://localhost:8008
# Production:  https://matrix.yourdomain.com
MATRIX_BASE_URL="http://localhost:8008"

# Server name (must match Synapse's server_name in homeserver.yaml)
# Development: localhost
# Production:  matrix.yourdomain.com (or just yourdomain.com)
MATRIX_SERVER_NAME="localhost"

# Admin access token for server-side operations
# Get this by logging in as admin user (see infra/matrix/README.md)
# curl -X POST -H "Content-Type: application/json" \
#   -d '{"type":"m.login.password","user":"admin","password":"xxx"}' \
#   http://localhost:8008/_matrix/client/v3/login
MATRIX_ADMIN_ACCESS_TOKEN=""

# Admin user ID (format: @username:server_name)
MATRIX_ADMIN_USER_ID="@admin:localhost"

# Shared secret for user registration (must match homeserver.yaml)
# Generate with: openssl rand -hex 32
MATRIX_SHARED_SECRET=""

# =============================================================================
# LIVEKIT (Video Calls)
# =============================================================================
# See infra/livekit/README.md for setup instructions

# LiveKit server URL (WebSocket)
# Development: ws://localhost:7880
# Production:  wss://livekit.yourdomain.com
LIVEKIT_URL="ws://localhost:7880"

# API Key and Secret for token generation
# Generate API key: openssl rand -base64 12 | tr -d '=+/' | head -c 12
# Generate secret:  openssl rand -base64 32
LIVEKIT_API_KEY=""
LIVEKIT_API_SECRET=""

# TURN server URLs for NAT traversal (comma-separated)
# Clients use these when direct connection fails
# Development: (leave empty, use Google STUN)
# Production:  turn:turn.yourdomain.com:3478,turns:turn.yourdomain.com:5349
TURN_URLS=""

# =============================================================================
# ENCRYPTION
# =============================================================================

# AES-256 key for encrypting Matrix access tokens in database
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=""
